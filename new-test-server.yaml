---
# noqa: yaml[indentation]
- name: Configure new-test-server
  hosts: localhost
  remote_user: deploy
  become: true
  roles:
  - { role: add_and_setup_deploy_user}
  - role: add_and_setup_deploy_user
    user_name: avi
    allow_sudo: false
    user_password: "{{ vault_avi_password }}"
    add_public_keys: []
# - { role: setup_internal_dns, become: yes }
# - { role: base_setup}
