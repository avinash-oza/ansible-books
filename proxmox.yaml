# noqa: yaml[empty-lines]

- name: Configure Proxmox
  hosts: proxmox
  remote_user: deploy

  roles:
#     - { role: base_setup, become: true }
    - { role: setup_smartmontools, become: true }
#     - { role: add_and_setup_deploy_user, become: true }
#   - { role: setup_apcupsd, become: true }
    # needs tags=all,proxmox to add proper monitoring
#     - { role: setup_nagios_monitoring, become: true }
