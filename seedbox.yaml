# noqa: yaml[empty-lines] yaml[indentation] yaml[truthy]

---
- hosts: seedbox
  remote_user: deploy
  become: yes
  roles:
    # chrony client setup intentionally missing
    - { role: setup_seedbox_firewall, become: yes }
